export const calcularOrcamento = async (formData) => {
  // Implementar a lógica de cálculo de orçamento aqui
  // Simulação de retorno do orçamento calculado
  return `O orçamento para sua instalação é R$ ${Math.random() * 1000}.00`;
};